{
  "name": "romy-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m uvicorn api.main:app --reload",
    "start": "python3 -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "fastapi": "^0.109.0",
    "uvicorn": "^0.27.0",
    "pydantic": "^2.5.0"
  },
  "engines": {
    "node": "24.x"
  },
  "private": true
}